source 'http://rubygems.org'

gem 'rails', '~>3.2'

gem 'blacklight', '~>3.4.0'
gem 'om', :git=>'git://github.com/barmintor/om.git', :branch=>'hydra-351'
gem 'hydra-head', '~>4.0.3'
gem 'cul_image_props', :git=>'git://github.com/cul/cul_image_props.git', :branch=>'master' # '>=0.2.3'
gem 'cul_scv_hydra', '>=0.5.1'
gem 'devise_wind'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'sqlite3'

gem 'devise'
gem 'devise-encryptable'

 group :assets do
   gem 'sass-rails', '~>3.2.0'
   gem 'compass-rails', '~>1.0.0'
   gem 'compass-susy-plugin', '~>0.9.0'
   gem 'jquery-rails'
 end

# Bundle gems for the local environment. Make sure to
# put test-only gems in this group so their generators
# and rake tasks are available in development mode:
 group :cucumber do
   gem 'cucumber'
   gem 'cucumber-rails'
 end

 group :development, :test do
   gem 'rspec-rails', '>=2.9.0'
   gem 'jettywrapper'
   gem 'database_cleaner'
 end

gem "compass-rails", "~> 1.0.0", :group => :assets
gem "compass-susy-plugin", "~> 0.9.0", :group => :assets
